<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mosirus.android.moviecatalog">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.mosirus.android.moviecatalog.CatalogApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieCatalog">
        <activity android:name="com.mosirus.android.moviecatalog.detail.TvShowDetailActivity" />
        <activity android:name="com.mosirus.android.moviecatalog.detail.MovieDetailActivity" />
        <activity android:name="com.mosirus.android.moviecatalog.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.mosirus.android.moviecatalog.home.HomeActivity" />
    </application>

</manifest>